export interface ISizes {
  navBarHeight: string;
  navBarTeethHeight: string;
  gutter: string;
}

const sizes: ISizes = {
  navBarHeight: '6rem',
  navBarTeethHeight: '2.5rem',
  gutter: '0.5rem',
};

export default sizes;
